#include <stdio.h>
int main() 
{ 
  int n1,n2;
  printf("Digite em sequencia os numeros que vc quer que sejam somados e -1 para finalizar de introduzir os numeros e realizar a soma ");
  while (n1 != -1) {
      scanf("%i",&n1);
      
    printf("%d\n", n1);
    n2 == n1 +n1;
  }
  printf("a soma é %d\n", n2);

  return 0;
}
